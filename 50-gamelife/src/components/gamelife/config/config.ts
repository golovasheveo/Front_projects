const settings = {
N_COLUMNS : 10,
N_ROWS : 10,
INTERVAL : 2000
}

export var N_COLUMNS = settings.N_COLUMNS;
export var N_ROWS = settings.N_ROWS;
export var INTERVAL = settings.INTERVAL;


export default settings;
